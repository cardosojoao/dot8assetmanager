export interface IMetadata {
    GeneratedBy: string;
    Enabled: boolean;
    Name: string;
    Path: string;
    Modified: string;
    Width: number;
    Height: number;
    Columns: number;
    Rows: number;
}